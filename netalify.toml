[build]
  command = "npm install && npm run build"
  publish = "."
  functions = "netlify-functions"

[dev]
  command = "npx serve -s ."
  targetPort = 3000

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
